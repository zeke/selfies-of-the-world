{
  "name": "selfies-of-the-world",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build:manifest": "node scripts/build-manifest.mjs",
    "dev": "npm run build:manifest && wrangler dev",
    "deploy": "npm run build:manifest && wrangler deploy"
  },
  "dependencies": {
    "hono": "^4.12.7"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260310.1",
    "typescript": "^5.9.3",
    "wrangler": "^4.72.0"
  }
}
